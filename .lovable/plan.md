````text
Цель: вернуть в каталог 3D-сканер BLZ LS200 Plus, который сейчас скрыт.
````

### Изменения

1. **Обновить `src/components/site/data.ts`**  
   У карточки `scanner-blz-ls200-plus` удалить строку `hidden: true` (сейчас она есть на строке 590).  
   Импорты изображений `blz-ls200-1.webp` … `blz-ls200-4.webp` уже присутствуют и остаются без изменений.

2. **Проверить фильтрацию**  
   `src/routes/catalog.index.tsx` исключает только товары с `hidden: true`. После удаления флага BLZ LS200 Plus снова появится в категории «3D-сканеры» и в общем каталоге.

3. **Проверить сборку**  
   Запустить `bun run build` и убедиться, что TypeScript проходит без ошибок.

### Как снова скрыть позднее

Добавить обратно `hidden: true` в объект сканера — товар исчезнет из каталога, но останется в коде для быстрого возврата.
